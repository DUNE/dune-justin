version: '3'

networks:
  internal:
volumes:
  mysql-db:

services:
  workflow:
    build:
      context: .
      dockerfile: justin/Dockerfile    
    # depends_on:
    volumes:
      - ./justin/workflowEntrypoint.sh:/usr/sbin/justinEntrypoint.sh
      - ../justin:/opt/app-root/src/justin
      - ./services/rucio.cfg:/opt/rucio/etc/rucio.cfg
    # links:
    #   - mysql:mysql
    networks:
      internal:
        aliases:
          - mariadb
