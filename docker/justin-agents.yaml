version: '3'

networks:
  internal:
volumes:
  maria-db:

services:
  justin:
    build:
      context: ..
      dockerfile: docker/justin/Dockerfile    
    # depends_on:
    volumes:
      - ./services/rucio.cfg:/opt/rucio/etc/rucio.cfg
    # links:
    #   - mysql:mysql
    networks:
      internal:
        aliases:
          - justin
